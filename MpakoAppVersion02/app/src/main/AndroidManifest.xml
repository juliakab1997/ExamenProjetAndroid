<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mpakoappversion02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MpakoAppVersion02">
        <activity android:name=".AgentActivity" />
        <activity android:name=".ClientActivity" />
        <activity
            android:name=".compteClient"
            android:label="@string/title_activity_compte_client" />
        <activity
            android:name=".CompteUser"
            android:label="@string/title_activity_compte_user" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>